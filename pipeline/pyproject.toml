[project]
name = "pa-housing-votes-pipeline"
version = "0.1.0"
description = "ETL for PA House district + municipality population data"
requires-python = ">=3.11"
dependencies = [
  "geopandas>=1.0",
  "shapely>=2.0",
  "pandas>=2.2",
  "requests>=2.32",
  "tqdm>=4.66",
]

[tool.ruff]
line-length = 100
target-version = "py311"
